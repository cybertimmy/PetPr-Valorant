import UIKit

extension URL {
    static let agentsURL = URL(string: Constants.agentsID)!
    static let weaponsURL = URL(string: Constants.weaponsID)!
    static let mapsURL = URL(string: Constants.mapsID)!
}
